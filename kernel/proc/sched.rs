// use super::{CUR_ENV,ENV_LIST};

pub fn schedule(y : bool) {
    // let cur_env_idx = CUR_ENV.lock();
    // let env_list = ENV_LIST.lock();
    // match *cur_env_idx {
    //     Some(idx) => {
    //         let mut cur_env = env_list.get_mut(idx).unwrap();
    //         if y {
    //             cur_env.status = EnvStatus::RUNNABLE;
    //         } else {
    //             cur_env.status = EnvStatus::SLEEPING;
    //         }
    //     },
    //     None => {
    //         panic!("No env is running");
    //     }
    // }
        loop {
            
        }
}
